# jedison-docs coverage audit (summary)

We compared what the `jedison` library can actually do against what `jedison-docs` currently explains. Below is what's missing, grouped by whether it's worth writing docs for.

## Public — real features, should be documented

None outstanding — everything from the original audit has been documented.
