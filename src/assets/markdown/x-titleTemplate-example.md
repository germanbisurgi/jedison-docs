```json
{
  "type": "array",
  "x-format": "nav-vertical",
  "items": {
    "type": "object",
    "x-titleTemplate": "{{ value.name || 'New contact' }}",
    "properties": {
      "name": { "type": "string" }
    }
  }
}
```
